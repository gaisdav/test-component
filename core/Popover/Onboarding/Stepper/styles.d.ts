import { Step as MuiStep } from '@material-ui/core';
interface Props {
    active: boolean;
}
export declare const Step: import("styled-components").StyledComponent<typeof MuiStep, import("styled-components").DefaultTheme, Props, never>;
export {};
